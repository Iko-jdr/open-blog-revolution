---
const { queryFilters, heading } = Astro.props;
import AllPosts from 'src/components/posts.astro';

---
<post-footer class="block mt-lg">
    <AllPosts queryFilters={queryFilters} heading={heading} />
</post-footer>